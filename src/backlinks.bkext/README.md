# Backlinks

Shows backlinks to the selected row in Bike's inspector.

The extension scans link attributes in the current outline and lists each row
that links to the selected row. Clicking a backlink reveals and selects that
source row in the editor.

Rows without a persistent id are reported as not linkable because Bike row links
target persistent ids.
